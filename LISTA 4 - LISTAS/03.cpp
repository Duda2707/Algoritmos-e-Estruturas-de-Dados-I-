//3 Fa¸ca um programa em C/C++ que, utilizando as TADs desenvolvidas em aula, concatene alternadamente duas listas encadeadas. Seu programa deve exibir como resultado uma lista L3 que
//seja resultado da concatena¸c˜ao de duas listas iniciais L1 e L2 (um item da L1 seguido de um item
//da L2, e assim sucessivamente).

#include <iostream>
#include <windows.h>
 #include "lista.hpp"
#include "lista.cpp"

using namespace std;
 
int main() {
  UINT CPAGE_UTF8 = 65001;
  UINT CPAGE_DEFAULT = GetConsoleOutputCP();
  SetConsoleOutputCP(CPAGE_UTF8);
 
  
 
 
  cout << endl << endl;
  return 0;
}